name: Legacy static Pages deployment

on:
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  disabled-note:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Automatic legacy Pages deployment is disabled. QuestLink is deployed by questlink-pages.yml."
